#include<stdio.h>
#include<conio.h>
int main ()

{
    int a,b,c;

    printf("enter angle of tringle :");
    scanf("%d%d%d",&a,&b,&c);

    if ((a+b)>c)

            if ((a+c)>b)

                    if ((c+b)>a)
                    {
                        printf("input are triangle");
                    }
                else
                {
                    printf("input are not triangle");
                }
                else
                {
                    printf("input are not triangle");
                }
                else
                {
                    printf("input are not triangle");
                }

}

